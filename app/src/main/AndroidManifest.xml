<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskPlussWidget"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.TaskPlussWidget">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AddTaskActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskPlussWidget" />

        <activity
            android:name=".SilentRefreshActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskPlussWidget.Transparent"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <activity
            android:name=".GroupSelectActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskPlussWidget.Transparent"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <activity
            android:name=".ToggleTaskActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskPlussWidget.Transparent"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <receiver
            android:name=".TaskPlussWidgetProvider"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <receiver
            android:name=".WidgetUpdateReceiver"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
